{{> layout/admin-layout-start}}

<div class="page-header">
    <h2>{{pageTitle}}</h2>
    <p>커뮤니티 게시판의 카테고리를 관리하세요</p>
</div>

<form method="post" action="/api/admin/view/community/categories" style="margin-bottom: 1rem;">
    <input type="text" name="name" placeholder="새 카테고리 이름" required>
    <button type="submit" class="btn btn-primary">추가</button>
</form>

<div class="table-container">
    <table class="data-table">
        <thead>
        <tr>
            <th>ID</th>
            <th>카테고리명</th>
            <th>관리</th>
        </tr>
        </thead>
        <tbody>
        {{#categories}}
            <tr>
                <td>{{id}}</td>
                <td>{{name}}</td>
                <td>
                    <button class="btn btn-danger" onclick="deleteCategory({{id}})">삭제</button>
                </td>
            </tr>
        {{/categories}}
        {{^categories}}
            <tr><td colspan="3" style="text-align:center;color:#999;">등록된 카테고리가 없습니다.</td></tr>
        {{/categories}}
        </tbody>
    </table>
</div>

<script src="/js/admin-category.js"></script>

{{> layout/admin-layout-end}}

server:
  servlet:
    tomcat:
      max-part-count: 20 # ???? ?? input ??? ??? ??
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
    encoding:
      charset: utf-8
      force: true
  port: 8080

# ??? ?? ? ?? ?? ??
upload:
  root-dir: ./uploads/
  counselor-dir: counselor-images/
  member-dir: member-images/

# ?? ??? ??
# ERROR > WARN > INFO > DEBUG > TRACE
logging:
  level:
    root: INFO  # ?? ?????? INFO ??? ??
    com.take: DEBUG # ? ????? DEBUG ?? ?? ??

# spring ?? ??
spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: tb.mailSender423@gmail.com
    password: dpfmgohpiuhhmevc
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  mustache:
    # ??? ??? ???? .mustache? ???? Mustache
    # ??? ??? ????? ??
    suffix: .mustache
    # ??? ??? ???
    # ?? ??? src/main/resources/templates/? ??
    prefix: classpath:/templates/
    # ?? ? ??? ??? ?????? ??
    # ?? ? ?? ????? ? (??????? true? ?? ??)
    cache: false
    # ??? ??? UTF-8 ????? ???? ?? ? ??? ?? ??
    charset: UTF-8
    servlet:
      # ??? ?? ??? ????? ?? ???? ?
      expose-session-attributes: true
      expose-request-attributes: true
  # ??? ??? ?? ?? (H2 ???? ??????)
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/take_breath?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: root
    password: asd1234


  # JPA ??
  jpa:
    hibernate:
      # create: ?????? ??? ???? ?? ??
      # ?? ???? ?? ??? (???)
      ddl-auto: create-drop
    # SQL ??? ??? ?? (??? ???)
    show-sql: true
    properties:
      hibernate:
        # SQL ??? ?? ?? ???
        format_sql: true
        # in query ??
        default_batch_fetch_size: 10
    # data.sql ??? Hibernate ??? ??? ??
    defer-datasource-initialization: true
# jwt
jwt:
  secret: 'VGhpc0lzQVNlY3JldEtleUZvckpXVFNhZmUhIT8xMjM0NTY='
  validityInMilliseconds: 3600000        # 1시간
  refresh-expiration-ms: 604800000     # 7일

# 포트원 설정
iamport:
  imp-code: ${IAMPORT_IMP_CODE}
  api-key: ${IAMPORT_API_KEY}
  api-secret: ${IAMPORT_API_SECRET}